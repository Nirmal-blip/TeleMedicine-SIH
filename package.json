{
  "name": "telemedicine-root",
  "private": true,
  "scripts": {
    "build": "cd backend && npm ci --no-audit --no-fund && npm run build",
    "start": "cd backend && npm run start:prod"
  },
  "dependencies": {
    "mongodb": "^6.19.0",
    "socket.io-client": "^4.8.1",
    "@nestjs/cli": "^11.0.10"
  }
}
